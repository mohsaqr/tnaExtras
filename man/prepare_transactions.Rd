% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/association_utils.R
\name{prepare_transactions}
\alias{prepare_transactions}
\title{Prepare Transaction Data for Association Rule Mining}
\usage{
prepare_transactions(data, item_cols = NULL, transaction_col = NULL)
}
\arguments{
\item{data}{Transaction data in various formats}

\item{item_cols}{Column names/indices for items (for data.frame input)}

\item{transaction_col}{Column name/index for transaction ID (for data.frame input)}
}
\value{
List with standardized transactions and metadata
}
\description{
Converts various input formats to standardized transaction format.
Supports list, matrix, and data.frame inputs.
}
\examples{
# List format - learning activities
learning_sessions <- list(c("plan", "discuss"), c("discuss", "execute"), c("plan", "execute"))
prepared <- prepare_transactions(learning_sessions)

# Data frame format - student actions
student_actions <- data.frame(
  student_id = c(1, 1, 2, 2, 3, 3),
  action = c("plan", "discuss", "discuss", "execute", "plan", "execute")
)
prepared <- prepare_transactions(student_actions, transaction_col = "student_id", item_cols = "action")
}
