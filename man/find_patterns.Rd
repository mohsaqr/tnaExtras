% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequence_motifs.R
\name{find_patterns}
\alias{find_patterns}
\alias{find_gapped_patterns}
\title{Find Patterns with Wildcards in Sequences}
\usage{
find_patterns(data, pattern = NULL, ...)

find_gapped_patterns(data, pattern = NULL, ...)
}
\arguments{
\item{data}{Data frame with sequences in wide format (rows are sequences,
columns are time points). Can also be a group_tna object.}

\item{pattern}{Pattern to search for with wildcards. Use:
\itemize{
\item \code{*} for single wildcard (matches exactly one state)
\item \code{**} for multi-wildcard (matches zero or more states)
}
Can be specified as vector: \code{c("A", "*", "B")} or string: \code{"A->*->B"}
If NULL, discovers frequent gapped patterns automatically.}

\item{min_gap}{Minimum gap size for discovered patterns (default: 1)}

\item{max_gap}{Maximum gap size for discovered patterns (default: 3)}

\item{min_support}{Minimum support threshold (default: 0.01)}

\item{min_count}{Minimum count threshold (default: 2)}

\item{test_significance}{Whether to perform significance tests (default: TRUE)}

\item{correction}{Multiple testing correction method (default: "fdr")}

\item{alpha}{Significance level (default: 0.05)}

\item{start_state}{Filter patterns starting with this state (default: NULL)}

\item{end_state}{Filter patterns ending with this state (default: NULL)}

\item{contains_state}{Filter patterns containing this state (default: NULL)}

\item{verbose}{Whether to print progress messages (default: TRUE)}
}
\value{
An object of class "sequence_patterns" containing:
\item{patterns}{Data frame of patterns with statistics}
\item{instances}{Data frame of actual pattern instances found}
\item{summary}{Summary statistics}
\item{parameters}{Analysis parameters}
}
\description{
Discovers patterns with wildcards/gaps in sequential data. Supports single
wildcards (*) matching any single state and multi-wildcards (**) matching
any number of states.
}
\examples{
\dontrun{
# Load regulation data
data <- tna::group_regulation
seq_data <- data[, -1]

# Search for specific gapped pattern
results <- find_patterns(seq_data, pattern = c("plan", "*", "consensus"))

# Multi-gap pattern
results <- find_patterns(seq_data, pattern = c("plan", "**", "plan"))

# Auto-discover gapped patterns
results <- find_patterns(seq_data, pattern = NULL, max_gap = 2)
}

}
